
<%= form_for @equipe do |f| %>

	<p>
		<%= f.label :nome %>
		<%= f.text_field :nome %>
		<%= error_tag @equipe, :nome %>
	</p>

	<div class="form-actions">
		<%= f.submit class: "btn btn-info" %>
	</div>

<% end %>